<%= pluralize(@users.size, 'user') %> found

<p>

<% @users.each do |user| %>
      <tr>
      	<br>
      
        <td><h4><%= user.name %></h4></td>
        <td><%= user.provider %></td><br>
        <td><% user.locations.each do |location| %></td>
          <%= location.address  %>

          <br>
        <% end %>
         <br>
        </td>
        <td class="actions">

          
        	<%= link_to("show", {:action => 'show', :id => user.id}, :class => 'action show') %>
        	<%= link_to("edit", {:action => 'edit', :id => user.id}, :class => 'action show') %>
        	<%= link_to("delete", {:action => 'delete', :id => user.id}, :class => 'action delete') %>
        </td>

       </tr>
        <% end %>

         <p>create a new user 
       <P>
          <%= link_to("new", {:action => 'new'}, :class => 'action new') %><br>
          <%= link_to('back', {:controller => 'locations', :action => 'index'}) %>

