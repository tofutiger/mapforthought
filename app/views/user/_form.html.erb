
<th><%= f.label(:name, "Name") %></th>
    <td><%= f.text_field(:name) %></td>
    <p>
    <th><%= f.label(:email, "Email ") %></th>
    <td><%= f.text_field(:email) %></td>
    <br>
    <p>

      
 
 <%= f.fields_for :locations do |builder| %>
        <fieldset>
    <%= builder.label :address %><br>
  <%= builder.country_select(:address) %>
        </fieldset>
        <% end %>  
<p>
  <% f.collection_select(:locations, Location.all, :id, :address, {prompt: "Select an Address"}) %>

  
<%= f.label(:provider, "What was the name of your provider?") %></th>
<br>
    <td><%= f.text_field(:provider) %></td>
<p>
    <%= f.label(:scholarships, "did you have any scholarships?") %></th>
    <br>
    <td><%= f.text_field(:scholarships) %></td>

<p>
  <%= f.label(:description, "description of your time!") %><br>
     <%= f.text_area :description, size: "60x12" %>

<p>
<p>
  
  <%= f.label(:avatar, "upload your avatar!") %><br>
  <%= f.file_field :avatar %>


 <p>

<%= f.fields_for :trip_images do |gallery| %>

<%= gallery.label :trip_images %><br>
  <%= gallery.file_field :trip_images %>
<% end %>

